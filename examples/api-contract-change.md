# API contract change

```sh
qwen-local
```

```text
Trace this API field through request validation, domain logic, persistence, events, clients, and observability. Identify backward-compatibility and partial-deploy risks before editing. Implement the smallest compatible change, pin any new dependency, update contract and integration tests, run the relevant checks, and commit the cohesive change.
```
